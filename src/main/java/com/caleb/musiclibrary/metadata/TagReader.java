package com.caleb.musiclibrary.metadata;

/**
 * Reads raw tag values from individual audio files.
 */
public class TagReader {
    // This will read tags from mp3 files and return them as a map of key-value pairs. 
    // It will be used by the MetadataCollector to gather all metadata for a given file.
}
